{
  "name": "x402check",
  "version": "0.1.0",
  "private": true,
  "description": "A paid x402 endpoint that runs a live conformance pre-flight check against another x402 endpoint.",
  "type": "module",
  "scripts": {
    "test": "node --test test/",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0"
  }
}
